export type Intro = {
  introText: string;
  profilePicUrl: string;
};

export type Project = {
  name: string;
  techStack: string[];
  imageSrc: string;
  liveUrl: string;
  sourceCodeUrl: string;
  description: string;
};
